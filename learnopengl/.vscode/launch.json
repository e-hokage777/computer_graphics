{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "CMake GDB Debug",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}", 
      "cwd": "${workspaceFolder}", // <--- THIS FORCES ROOT FOLDER RUNNING
      "MIMode": "gdb"
    }
  ]
}